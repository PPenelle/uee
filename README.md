# Uniform Error Estimator (UEE)

UEE is a Python implementation of minimax (Chebyshev) regression.

It estimates a regression model that minimizes the worst-case (maximum) prediction error instead of the average squared error used in ordinary least squares (OLS) and provide inference.

------------------------------------------------------------

# Legacy repository notice

> **Legacy code notice**
>
> This repository contains the original, pre-packaged development version of the Uniform Error Estimator.
> The actively maintained, installable package now lives at:
>
> - Source repository: https://github.com/PPenelle/uniform-error-estimator
> - PyPI package: `uee`
>
> Install the maintained package with:
>
> ```bash
> pip install uee
> ```
>
> This legacy repository is retained for historical reference only.
